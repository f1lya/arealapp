const getters = {
    isModalVisibleAdd(state) {
        return state.isModalVisibleAdd
    },
    isModalVisibleUpdate(state) {
        return state.isModalVisibleUpdate
    }
}
export default getters